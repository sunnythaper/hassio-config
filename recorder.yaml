purge_interval: 2
purge_keep_days: 5
include:
    domains:
      - automation
      - binary_sensor
      - climate
      - device_tracker
      - input_select
      - media_player
      - switch
      - calendar
      - light
      - sensor
      - sun
